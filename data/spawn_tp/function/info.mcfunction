say provides a trigger that when activated teleports the player back to spawn
say to setup run the builld_spawn function while standing where people will teleport to (spawn)
say if you want to remove spawn, kill marker named "spawn" and re run builld_spawn